apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "pay-web-ingress"
  namespace: "prod-non-pci-tara"
spec:
  ingressClassName: "public"
  tls:
    - secretName: "indepay-certs"
  rules:
    - host: "pay.tara.app"
      http:
        paths:
          - pathType: "ImplementationSpecific"
            backend:
              service:
                name: "pay-service-prod-prod"
                port:
                  number: 80
