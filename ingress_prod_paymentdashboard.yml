apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "dashboard-web-ingress"
  namespace: "prod-non-pci-tara"
spec:
  ingressClassName: "public"
  tls:
    - secretName: "indepay-certs"
  rules:
    - host: "bp-dashboard.tara.app"
      http:
        paths:
          - pathType: "ImplementationSpecific"
            backend:
              service:
                name: "payment-dashboard-prod-prod"
                port:
                  number: 80