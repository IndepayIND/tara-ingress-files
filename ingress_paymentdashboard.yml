apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: "dashboard-web-ingress"
  namespace: "dev-tara"
spec:
  ingressClassName: "public"
  tls:
    - secretName: "indepay-certs"
  rules:
    - host: "dashboard-dev.tara.app"
      http:
        paths:
          - pathType: "ImplementationSpecific"
            backend:
              service:
                name: "payment-dashboard-dev-dev"
                port:
                  number: 80
